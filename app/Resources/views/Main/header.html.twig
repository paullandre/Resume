{% block header %}
<!-- Main Header -->
<header class="main-header" style="background-color: #222d32 !important;">
    <!-- Logo -->
    <a href="{{ path('home')}}" class="logo">
        <!-- mini logo for sidebar mini 50x50 pixels -->
        <span class="logo-mini"><b>1</b>43</span>
        <!-- logo for regular state and mobile devices -->
        <span class="logo-lg" id="clock"></span>
    </a>

    <!-- Header Navbar -->
    <nav class="navbar navbar-static-top" role="navigation">
        <!-- Sidebar toggle button-->
        <a href="#" class="sidebar-toggle" data-toggle="push-menu" role="button">
            <span class="sr-only">Toggle navigation</span>
        </a>
        <!-- Navbar Right Menu -->
		<!--
        <div class="navbar-custom-menu">
            <ul class="nav navbar-nav">                                
                <li class="dropdown user user-menu">                    
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">                        
                        <img src="{{ asset('AdminLTE/dist/img/ako.jpg')}} " class="user-image" alt="User Image">                        
                        <span class="hidden-xs">Paul Andre Francisco</span>
                    </a>
                    <ul class="dropdown-menu">                        
                        <li class="user-header">
                            <img src="{{ asset('AdminLTE/dist/img/ako.jpg')}}" class="img-circle" alt="User Image">

                            <p>
                                Paul Andre Francisco - Web Developer
                                <small>Member since Nov. 2012</small>
                            </p>
                        </li>                                                
                        <li class="user-footer">
                            <div class="pull-left">
                                <a href="#" class="btn btn-default btn-flat">Profile</a>
                            </div>
                            <div class="pull-right">
                                <a href="#" class="btn btn-default btn-flat">Sign out</a>
                            </div>
                        </li>
                    </ul>
                </li>                                      
                <li>
                    <a href="#" data-toggle="control-sidebar"><i class="fa fa-gears"></i></a>
                </li>
            </ul>
        </div>
		-->
    </nav>
</header>
<script src="{{ asset('AdminLTE/bower_components/jquery/dist/jquery.min.js') }}"></script>
<script>
    function display_c()
    {
        var refresh = 1000; // Refresh rate in milli seconds
        mytime = setTimeout('current_time()',refresh);
    }

    function current_time()
    {
        let strcount;
        let x = new Date();        
        
        let month = x.getMonth() + 1;
        month = month < 10 ? "0" + month : month;        
        let day = x.getUTCDate();
        let year = x.getFullYear();
        let y = month + "/" + day + "/" + year;

		//var event = new Date(Date.UTC(year, month, day, 0, 0, 0));
		//var options = { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' };
        //console.log(event.toLocaleDateString('fil-PH', options));	
		//let z = event.toLocaleDateString('fil-PH', options);
		
        let hours = x.getHours();
		
		if(hours > 12 && hours <= 23)
		{
			hours = hours - 12;
		}
		
        if(hours == 0 || hours < 10)
        {            
            hours = "0" + hours;
        }		      
        
        let seconds = x.getSeconds() < 10 ? "0" + x.getSeconds() : x.getSeconds();
        let minutes = x.getMinutes() < 10 ? "0" + x.getMinutes() : x.getMinutes();
        let merediem = x.getHours() > 12 ? "PM" : "AM";
        
        let me = "&nbsp;&nbsp;&nbsp;&nbsp; " + hours + ":" + minutes + ":";		
                
        $("#clock").html(y + "<i>" + me + "</i><i style='color: cyan;'>" + seconds + "</i>" 
		+ "<i style='color: BlueViolet; font-size: 17px;'>&nbsp;&nbsp" + merediem + "</i>");
        
        tt = display_c();
    }
    
    $(document).ready(function()
    {
        current_time();					
    });
</script>
<style>    
    #mil { letter-spacing: 3px; }
    #clock 
    {
{#        letter-spacing: 1px;#}
        margin: 1.4em 0 0 -2.5em; 
{#        color: #6600CC;#}
        color: #FFF;
        font: 13px/15px "Trebuchet MS",Geneva,Arial,Helvetica,SunSans-Regular,sans-serif; 
    }
    
    #profile 
    { 
        margin: 0 0 0 -1em; 
        letter-spacing: 1px; 
        color: #1b6d85; 
        
    }
</style>
{% endblock %}