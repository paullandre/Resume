{% block header %}
<!-- Main Header -->
<header class="main-header">
    <!-- Logo -->
    <a href="index2.html" class="logo">
        <!-- mini logo for sidebar mini 50x50 pixels -->
        <span class="logo-mini"><b>A</b>LT</span>
        <!-- logo for regular state and mobile devices -->
        <span class="logo-lg" id="clock"></span>
    </a>

    <!-- Header Navbar -->
    <nav class="navbar navbar-static-top" role="navigation">
        <!-- Sidebar toggle button-->
        <a href="#" class="sidebar-toggle" data-toggle="push-menu" role="button">
            <span class="sr-only">Toggle navigation</span>
        </a>
        <!-- Navbar Right Menu -->
        <div class="navbar-custom-menu">
            <ul class="nav navbar-nav">                
                <!-- User Account Menu -->
                <li class="dropdown user user-menu">
                    <!-- Menu Toggle Button -->
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <!-- The user image in the navbar-->
                        <img src="{{ asset('AdminLTE/dist/img/ako.jpg')}} " class="user-image" alt="User Image">
                        <!-- hidden-xs hides the username on small devices so only the image appears. -->
                        <span class="hidden-xs">Paul Andre Francisco</span>
                    </a>
                    <ul class="dropdown-menu">
                        <!-- The user image in the menu -->
                        <li class="user-header">
                            <img src="{{ asset('AdminLTE/dist/img/ako.jpg')}}" class="img-circle" alt="User Image">

                            <p>
                                Paul Andre Francisco - Web Developer
                                <small>Member since Nov. 2012</small>
                            </p>
                        </li>                        
                        <!-- Menu Footer-->
                        <li class="user-footer">
                            <div class="pull-left">
                                <a href="#" class="btn btn-default btn-flat">Profile</a>
                            </div>
                            <div class="pull-right">
                                <a href="#" class="btn btn-default btn-flat">Sign out</a>
                            </div>
                        </li>
                    </ul>
                </li>                      
                <!-- Control Sidebar Toggle Button -->
                <li>
                    <a href="#" data-toggle="control-sidebar"><i class="fa fa-gears"></i></a>
                </li>
            </ul>
        </div>
    </nav>
</header>
<script src="{{ asset('AdminLTE/bower_components/jquery/dist/jquery.min.js') }}"></script>
<script>
    function display_c()
    {
        var refresh = 1000; // Refresh rate in milli seconds
        mytime = setTimeout('current_time()',refresh);
    }

    function current_time()
    {
        let strcount;
        let x = new Date();        
        
        let month = x.getMonth() + 1;
        month = month < 10 ? "0" + month : month;        
        let day = x.getUTCDate();
        let year = x.getFullYear();
        let y = month + "/" + day + "/" + year;                
        
        let hours = x.getHours();
		
		if(hours > 12 && hours <= 23)
		{
			hours = hours - 12;
		}
		
        if(hours == 0 || hours < 10)
        {            
            hours = "0" + hours;
        }		      
        
        let seconds = x.getSeconds() < 10 ? "0" + x.getSeconds() : x.getSeconds();
        let minutes = x.getMinutes() < 10 ? "0" + x.getMinutes() : x.getMinutes();
        let merediem = x.getHours() > 12 ? "PM" : "AM";
        
        let me = "&nbsp;&nbsp;&nbsp;&nbsp; " + hours + ":" + minutes + ":" + seconds  + " " + merediem;                
                
        $("#clock").html(y + "<i>" + me + "</i>");
        
        tt = display_c();
    }
    
    $(document).ready(function()
    {
        current_time();
    });
</script>
<style>
    #mil { letter-spacing: 3px; }
    #clock 
    {
{#        letter-spacing: 1px;#}
        margin: 1.4em 0 0 -3em; 
        color: #6600CC;
        font: 13px/15px "Trebuchet MS",Geneva,Arial,Helvetica,SunSans-Regular,sans-serif; 
    }
    
    #profile 
    { 
        margin: 0 0 0 -1em; 
        letter-spacing: 1px; 
        color: #1b6d85; 
        
    }
</style>
{% endblock %}