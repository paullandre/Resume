{% block aside %}
<aside class="main-sidebar">    
    <section class="sidebar">               
        <ul class="sidebar-menu" data-widget="tree">
            <li class="header" id="sidehead">HIGHLIGHTS</li>            
            {% for data in menu %}
                {% if loop.index == 1 %}
                    <li class="active"><a href="#"><i class="{{ data.classname }}"></i> <span>{{ data.name }}</span></a></li>
                {% else %}
                    <li><a href="#"><i class="{{ data.classname }}"></i> <span>{{ data.name }}</span></a></li>
                {% endif %}                                        
            {% endfor %}                        
        </ul>        
    </section>    
</aside>
<script src="{{ asset('AdminLTE/bower_components/jquery/dist/jquery.min.js') }}"></script>
<script>
    $(document).ready(function()
    {
        /*$.ajax(
        {
            type: "POST",
            url: "", 
            data: {"data": data} ,
            success: function(result)
            {
                
            }
        });*/
    });
</script>
{% endblock %}